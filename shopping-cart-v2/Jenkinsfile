pipeline {
    agent { node { label 'maven' } }
    environment { APP_NAMESPACE = 'owpipt-recover' }
    stages {
        stage('Test') {
            options { timeout(time: 255, unit: 'SECONDS') }
            steps {
                dir('shopping-cart-v2') {
                    sh './mvnw clean test'
                }
            }
        }
    }
}
